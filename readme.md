## Instalation

```
python -m venv venv 
-- on Windows
venv\Scripts\activate 
-- on linux & MAC
source venv/bin/activate
pip install -r requirements.txt 
```

## Start app

```
uvicorn main:app --reload
```
